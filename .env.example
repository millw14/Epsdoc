# Server Configuration
PORT=3001

# Database path (relative to project root)
DB_PATH=document_analysis.db

# Database download URL (required for Railway deployment)
# Host your database file and provide the direct download URL
# Options: GitHub Releases, Google Drive, Dropbox, etc.
DB_URL=https://your-file-host.com/document_analysis.db

# CORS origins (comma-separated, optional - defaults to localhost)
# ALLOWED_ORIGINS=https://your-domain.com

# Groq API Key for AI features (required for AI interrogation feature)
VITE_GROQ_API_KEY=your_groq_api_key_here
